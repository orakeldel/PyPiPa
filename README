PyPiPa

PyPiPa is a multiplayer multigame arcade game. You control several arcade games at once with only 4 keys (i.e. cursor keys).

PyPiPa was originally planned to implement in Python and is likely to be implemented in HTML5 and JS instead.
